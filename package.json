{
  "name": "esti-mate",
  "version": "3.0.3",
  "description": "Your Harvest estimate's best friend",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "rollup -c rollup.config.mjs",
    "watch": "rollup -c rollup.config.mjs -w",
    "dev": "node scripts/dev.mjs",
    "start": "web-ext run --source-dir ./dist/",
    "lint": "web-ext lint --source-dir ./dist/",
    "lint:fix": "eslint --fix src/",
    "version:patch": "npm version patch --no-git-tag-version && node scripts/sync-manifest-version.cjs",
    "version:minor": "npm version minor --no-git-tag-version && node scripts/sync-manifest-version.cjs",
    "version:major": "npm version major --no-git-tag-version && node scripts/sync-manifest-version.cjs",
    "prepare-release": "npm run build && rm -f dist/dummy.js && mkdir -p releases && cd dist && zip -r ../releases/esti-mate-v$(node -p \"require('../package.json').version\").zip . && cd .. && npm run source-code && echo \"✅ Release package created: releases/esti-mate-v$(node -p \"require('./package.json').version\").zip\" && echo \"✅ Source code package created: releases/esti-mate-source-v$(node -p \"require('./package.json').version\").zip\"",
    "source-code": "mkdir -p releases && git archive --format=zip --output releases/esti-mate-source-v$(node -p \"require('./package.json').version\").zip HEAD -- . ':(exclude)AGENTS.md' ':(exclude)CLAUDE.md' ':(exclude)node_modules/**' ':(exclude)dist/**' ':(exclude)releases/**' ':(exclude)*.zip' ':(exclude).git/**' ':(exclude).idea/**' ':(exclude)doc/**' ':(exclude)todos/**' ':(exclude).claude/**' ':(exclude).codex/**' ':(exclude)server/**' ':(exclude).env.*' ':(exclude)**/.DS_Store' && echo '✅ Source code package ready for Firefox submission'"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "@rollup/plugin-commonjs": "^26.0.1",
    "@rollup/plugin-node-resolve": "^15.2.3",
    "eslint": "^9.9.0",
    "globals": "^15.9.0",
    "rollup": "^4.22.4",
    "rollup-plugin-copy": "^3.5.0"
  }
}
